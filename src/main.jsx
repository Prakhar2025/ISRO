import React from 'react'
import ReactDOM from 'react-dom/client'
import CMEDashboard from '../CMEDashboard.jsx'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <CMEDashboard />
  </React.StrictMode>,
)
